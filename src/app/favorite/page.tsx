type metadata = {
  title: string;
  description?: string;
};
export const metadata: metadata = {
  title: "eShop || Favorites",
};

const favorite = () => {
  return <section className="container">favorite</section>;
};

export default favorite;
