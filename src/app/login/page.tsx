type metadata = {
  title: string;
  description?: string;
};
export const metadata: metadata = {
  title: "eShop || Login",
};

const login = () => {
  return <section className="container">login</section>;
};

export default login;
