type metadata = {
  title: string;
  description?: string;
};
export const metadata: metadata = {
  title: "eShop || Cart",
};

const cart = () => {
  return <section className="container">cart</section>;
};

export default cart;
